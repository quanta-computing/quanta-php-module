#!/usr/bin/make -f

build:
	phpize
	dh_build

override_dh_auto_install:
	$(MAKE) INSTALL_ROOT=$$(pwd)/debian/php5-quanta-mon install

override_dh_shlibdeps:
	dh_shlibdeps --dpkg-shlibdeps-params=--ignore-missing-info

%:
	dh $@
